// Server-side placeholder removed; concrete event type lives in Content.Shared to allow prototype loader to resolve it.
// If server-specific logic is needed, implement it in a separate class that does not conflict with the shared type.


